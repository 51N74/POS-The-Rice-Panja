{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      // 💡 แก้ไข Alias ให้ชี้ไปที่ /app/lib
      "@/app/lib/*": ["app/lib/*"],
      // 💡 Alias เดิมที่ Vercel ไม่รู้จักคือ "@/lib/prisma"
      // หากคุณต้องการใช้ "@/lib/prisma" คุณต้องกำหนดให้ชี้ไปที่ app/lib/prisma.js
      "@/lib/*": ["app/lib/*"],
      // 🔑 แนะนำให้เปลี่ยนในไฟล์ route.js ให้เป็น "@/app/lib/prisma" เพื่อให้ตรงกับโค้ดด้านล่าง
      // แต่ถ้าอยากใช้ @/lib/prisma ต้องเพิ่ม Path นี้
      "@/lib/prisma": ["app/lib/prisma.js"]
    }
  },
  "include": ["**/*.js", "**/*.jsx"]
}
